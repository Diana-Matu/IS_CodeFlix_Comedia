---
import Header from "../components/Header.astro";
// import { ClientRouter } from "astro:transitions";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/movieicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>CodeFlix :D</title>

    <!-- Transiciones entre páginas -->
    <!-- <ClientRouter transition:name="fade" /> -->
  </head>

  <body class="layout-body">
    <!-- Header global -->
    <Header />

    <!-- Contenido dinámico -->
    <main class="max-w-5xl px-4 m-auto page">
      <slot />
      <!-- <Astro.Scripts /> -->
    </main>
  </body>
</html>

<style is:global>
  /* === CARD ESTILOS ORIGINALES — GLOBAL === 
  .card {
    display: flex;
    flex-direction: column;
    background-color: #000;
    color: #fff;
    border-radius: 12px;
    overflow: hidden;
    text-decoration: none;
    transition: transform .25s, box-shadow .25s;
  }

  .card:hover {
    transform: scale(1.06);
    box-shadow: 0 0 15px 6px #ffea00;
  }

  .card-img img,
  .card img {
    width: 100%;
    height: 340px;
    object-fit: cover;
    display: block;
  }

  .card-info {
    padding: 0.8rem;
    text-align: center;
  }

  .card-info .title {
    color: #ffea00;
    font-size: 1.05rem;
    font-weight: 700;
    margin: 0.25rem 0;
  }

  .card-info .rating {
    color: #ddd;
    margin: 0.25rem 0;
  }

  .card-info .overview {
    color: #aaa;
    font-size: .85rem;
    line-height: 1.2;
    max-height: 3.6em;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  */
</style>

